/**
 * @deprecated Import from '../shared/middleware/error-handler.middleware' instead
 * Bridge file for backwards compatibility
 */
export {
  AppError,
  errorHandler,
  notFoundHandler,
  default
} from '../shared/middleware/error-handler.middleware';
