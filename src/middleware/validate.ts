/**
 * @deprecated Import from '../shared/middleware/validator.middleware' instead
 * Bridge file for backwards compatibility
 */
export { validate, validateRequest } from '../shared/middleware/validator.middleware';
