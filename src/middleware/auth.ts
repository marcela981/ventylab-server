/**
 * @deprecated Import from '../shared/middleware/auth.middleware' instead
 * Bridge file for backwards compatibility
 */
export {
  authenticate,
  optionalAuth,
  requireRole,
  requireAdmin,
  requireTeacherPlus,
  default
} from '../shared/middleware/auth.middleware';
